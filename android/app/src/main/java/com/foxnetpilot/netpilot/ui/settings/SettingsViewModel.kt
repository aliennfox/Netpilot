package com.foxnetpilot.netpilot.ui.settings

import androidx.lifecycle.ViewModel

class SettingsViewModel : ViewModel() {
    // TODO(A2 后续): subscriptions / failover / backup / apiToken state + actions
}
